{
  "prefix": "avatar",
  "tokens": {
    "size": {
      "radius": {
        "default": "{core.shape.radius.full}"
      }
    },
    "color": {
      "border": {
        "default": "{semantic.color.border.light}"
      }
    }
  }
}

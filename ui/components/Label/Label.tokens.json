{
  "prefix": "label",
  "tokens": {
    "color": {
      "text": {
        "default": "{semantic.color.foreground.primary}"
      }
    },
    "typo": {
      "weight": {
        "default": "{semantic.typography.fontWeight.medium}"
      },
      "lineHeight": {
        "default": "{semantic.typography.lineHeight.tight}"
      }
    }
  }
}

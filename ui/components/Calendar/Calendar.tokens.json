{
  "prefix": "calendar",
  "tokens": {
    "color": {
      "background": {
        "default": "{semantic.color.background.secondary}",
        "imageOverlay": "{semantic.color.background.imageOverlay}"
      },
      "foreground": {
        "primary": "{semantic.color.foreground.primary}",
        "secondary": "{semantic.color.foreground.secondary}"
      },
      "border": {
        "default": "{semantic.color.border.light}",
        "accent": "{semantic.color.border.accent}"
      }
    },
    "size": {
      "padding": {
        "default": "{core.spacing.size.07}"
      },
      "radius": {
        "default": "{core.shape.radius.medium}"
      }
    },
    "elevation": {
      "default": "{semantic.elevation.default}",
      "hover": "{semantic.elevation.depth.2}"
    },
    "motion": {
      "duration": "{core.motion.duration.short}",
      "easing": "{core.motion.easing.standard}"
    }
  }
}

{
  "prefix": "input",
  "tokens": {
    "color": {
      "bg": { "default": "{semantic.color.background.secondary}" },
      "text": { "default": "{semantic.color.foreground.primary}" },
      "border": { "default": "{semantic.color.border.default}" },
      "focus": { "default": "{semantic.color.border.focus}" },
      "invalid": { "default": "{semantic.color.border.danger}" }
    },
    "size": {
      "height": { "default": "{core.spacing.size.06}" },
      "radius": { "default": "{core.shape.radius.medium}" }
    },
    "space": {
      "inline": { "default": "{core.spacing.size.03}" }
    }
  }
}

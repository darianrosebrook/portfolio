{
  "prefix": "truncate",
  "tokens": {
    "color": {
      "foreground": {
        "link": "{semantic.color.foreground.link}",
        "linkHover": "{semantic.color.foreground.linkHover}"
      },
      "background": {
        "primary": "{semantic.color.background.primary}"
      }
    },
    "typography": {
      "fontWeight": "{semantic.typography.fontWeight.medium}"
    },
    "spacing": {
      "toggle": "{core.spacing.size.02}"
    }
  }
}

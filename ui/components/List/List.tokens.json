{
  "prefix": "list",
  "tokens": {
    "color": {
      "foreground": {
        "primary": "{semantic.color.foreground.primary}",
        "secondary": "{semantic.color.foreground.secondary}"
      },
      "border": {
        "default": "{semantic.color.border.subtle}"
      }
    },
    "typography": {
      "sm": {
        "fontSize": "{semantic.typography.body.04}",
        "lineHeight": "{semantic.typography.lineHeight.body}"
      },
      "md": {
        "fontSize": "{semantic.typography.body.03}",
        "lineHeight": "{semantic.typography.lineHeight.body}"
      },
      "lg": {
        "fontSize": "{semantic.typography.body.02}",
        "lineHeight": "{semantic.typography.lineHeight.body}"
      }
    },
    "size": {
      "padding": {
        "default": "{core.spacing.size.06}"
      },
      "border": {
        "default": "{core.shape.border.width.hairline}"
      }
    },
    "spacing": {
      "inline": "{core.spacing.size.04}",
      "divided": "{core.spacing.size.05}",
      "spaced": "{core.spacing.size.06}",
      "sm": "{core.spacing.size.03}",
      "md": "{core.spacing.size.05}",
      "lg": "{core.spacing.size.06}",
      "dl": {
        "dt": "{core.spacing.size.03}",
        "dd": "{core.spacing.size.05}",
        "gap": "{core.spacing.size.03}",
        "gapX": "{core.spacing.size.05}"
      }
    }
  }
}

{
  "prefix": "image",
  "tokens": {
    "color": {
      "background": {
        "default": "{semantic.color.background.secondary}",
        "error": "{semantic.color.background.dangerSubtle}"
      },
      "foreground": {
        "primary": "{semantic.color.foreground.primary}",
        "secondary": "{semantic.color.foreground.secondary}",
        "error": "{semantic.color.foreground.danger}"
      }
    },
    "size": {
      "xs": "{core.spacing.size.08}",
      "sm": "{core.spacing.size.10}",
      "md": "{core.spacing.size.12}",
      "lg": "{core.spacing.size.14}",
      "xl": "{core.spacing.size.16}",
      "icon": "{core.spacing.size.08}"
    },
    "radius": {
      "none": {
        "value": 0,
        "unit": "px"
      },
      "sm": "{core.shape.radius.small}",
      "md": "{core.shape.radius.medium}",
      "lg": "{core.shape.radius.large}",
      "full": "{core.shape.radius.full}"
    },
    "typography": {
      "error": {
        "fontSize": "{semantic.typography.body.04}"
      }
    }
  }
}

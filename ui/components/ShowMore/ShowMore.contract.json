{
  "name": "ShowMore",
  "layer": "composer",
  "anatomy": [
    "root",
    "content",
    "trigger"
  ],
  "variants": {},
  "states": [
    "default"
  ],
  "slots": {
    "content": {
      "required": false
    },
    "trigger": {
      "required": false
    }
  },
  "a11y": {
    "role": "button",
    "labeling": [],
    "keyboard": [
      {
        "key": "Enter|Space",
        "when": "trigger",
        "then": "activate"
      }
    ],
    "apgPattern": "button"
  },
  "tokens": {
    "root": {
      "showmore.color.background.default": {
        "resolvesTo": "semantic.color.background.secondary",
        "fallback": "#efefef",
        "property": "background-color",
        "layer": "semantic"
      },
      "showmore.color.foreground.primary": {
        "resolvesTo": "semantic.color.foreground.primary",
        "fallback": "#141414",
        "property": "color",
        "layer": "semantic"
      },
      "showmore.color.foreground.secondary": {
        "resolvesTo": "semantic.color.foreground.secondary",
        "fallback": "#555555",
        "property": "color",
        "layer": "semantic"
      },
      "showmore.color.border.default": {
        "resolvesTo": "semantic.color.border.light",
        "fallback": "#fceaea",
        "property": "border-color",
        "layer": "semantic"
      },
      "showmore.color.border.accent": {
        "resolvesTo": "semantic.color.border.accent",
        "fallback": "#d9292b",
        "property": "border-color",
        "layer": "semantic"
      },
      "showmore.size.padding.default": {
        "resolvesTo": "core.spacing.size.07",
        "fallback": "24px",
        "property": "padding",
        "layer": "core"
      },
      "showmore.size.radius.default": {
        "resolvesTo": "core.shape.radius.medium",
        "fallback": "8px",
        "property": "border-radius",
        "layer": "core"
      },
      "showmore.elevation.default": {
        "resolvesTo": "semantic.elevation.default",
        "fallback": "[object Object]",
        "layer": "semantic"
      },
      "showmore.overlay.imageOverlay": {
        "resolvesTo": "semantic.color.background.image.overlay",
        "fallback": "rgba(0, 0, 0, 0.5)",
        "layer": "semantic"
      }
    }
  },
  "props": {
    "designed": {
      "members": [
        {
          "name": "defaultValue",
          "type": "string | number | readonly string[] | undefined",
          "description": "defaultValue prop for ShowMore.",
          "required": false
        },
        {
          "name": "title",
          "type": "string | undefined",
          "description": "title prop for ShowMore.",
          "required": false
        }
      ]
    },
    "passthrough": {
      "extends": "div"
    }
  },
  "ssr": {
    "hydrateOn": "interaction"
  },
  "rtl": {
    "flipIcon": false
  },
  "description": "Expandable container that truncates long content with a \"show more\" affordance.",
  "a2ui": {
    "category": "content",
    "usageHints": [
      "Use when content could be long but brevity is preferred by default",
      "Configure maxLines to control how many lines are shown before truncation",
      "Avoid if the full content is always needed \u2014 prefer Details instead"
    ],
    "children": {
      "allowed": true,
      "slot": "children",
      "accepts": [
        "ReactNode"
      ]
    }
  }
}

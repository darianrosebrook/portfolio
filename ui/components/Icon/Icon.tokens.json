{
  "prefix": "icon",
  "tokens": {
    "color": {
      "foreground": {
        "default": "{semantic.color.foreground.primary}"
      }
    },
    "size": {
      "padding": {
        "default": "{core.spacing.size.01}"
      }
    }
  }
}

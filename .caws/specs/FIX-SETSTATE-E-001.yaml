id: FIX-SETSTATE-E-001
title: Extract useIsMounted hook and refactor SSR-guard consumers
risk_tier: 3
mode: fix
created_at: '2026-05-20T22:15:40.440Z'
updated_at: '2026-05-20T22:15:40.440Z'
worktree: fix-setstate-e
blast_radius:
  modules:
    - app/heroes
    - app/BlueprintsWrapper
    - ui/components/PageTransition
    - utils/hooks
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - utils/hooks/**
    - app/BlueprintsWrapper.tsx
    - app/heroes/GlossaryCardsWrapper.tsx
    - app/heroes/SwatchesWrapper.tsx
    - ui/components/PageTransition/PageTransition.tsx
  out:
    - node_modules/
    - dist/
    - build/
invariants:
  - SSR-guarded components still avoid hydration mismatch
  - PageTransition still triggers its transition class on pathname change
  - No flash-of-server-content (placeholders still render server-side)
acceptance:
  - id: A1
    given: the closed historical change
    when: the work was completed
    then: 0 react-hooks/set-state-in-effect findings in the 4 in-scope consumer files
  - id: A2
    given: the closed historical change
    when: the work was completed
    then: A reusable useIsMounted (or equivalent) hook exists under utils/hooks/ and is exported
  - id: A3
    given: the closed historical change
    when: the work was completed
    then: tsc --noEmit clean; any existing tests for the touched components still pass
non_functional:
  security: []
  accessibility: []
  performance: []
contracts: []
lifecycle_state: closed
resolution: completed

id: FEAT-EDITOR-FIELDS-001
title: 'Move the editor properties fields onto the Input primitive'
risk_tier: 2
mode: refactor
lifecycle_state: active
worktree: editor-fields-001
created_at: '2026-09-21T14:50:20.263Z'
updated_at: '2026-09-21T14:50:21.276Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'dashboard editor — properties fields'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'app/dashboard/_components/ContentEditor.tsx'
  out: []
invariants:
  - 'Each field keeps its accessible name from its explicit label and keeps rendering the record value'
  - 'The description stays a native textarea: no Textarea primitive exists'
  - 'Typing still updates the controlled state that feeds the save and publish payloads'
acceptance:
  - id: A1
    given: 'the properties panel'
    when: 'it renders'
    then: 'all seven inputs are the design system Input with their accessible names intact (slug, headline, image, articleSection, keywords, published_at, scheduled_at)'
  - id: A2
    given: 'a field with a stored value'
    when: 'it renders'
    then: 'it shows the record value, and typing updates the field without saving'
  - id: A3
    given: 'the swap'
    when: 'the fields are measured at the same viewport on both builds'
    then: 'the appearance delta is the design system delta and no horizontal overflow appears'
non_functional: {}
contracts:
  - name: 'dashboard-editor'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Runtime on the worktree build: all seven ids (slug, headline, image, articleSection, keywords, published_at, scheduled_at) report data-ds-component \"Input\", each with its label association intact — the measured label texts are Slug, Headline, Image, Section, Keywords (comma-separated), Published at and Schedule for. No style attribute on any field."
    recorded_at: "2026-09-21T14:53:56.087Z"
    command: "browser: inspect all seven fields on the worktree build"
    commit_sha: "06ecea17b572fa0e8df201168b50ab7c43cdfccb"
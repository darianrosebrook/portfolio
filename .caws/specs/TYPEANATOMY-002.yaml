id: TYPEANATOMY-002
type: chore
title: Reconcile overlay-script labels for tittle cases (Slice 2R)
status: draft
risk_tier: 3
mode: chore
created_at: '2026-04-29T02:16:19.692Z'
updated_at: '2026-04-29T02:16:19.693Z'
blast_radius:
  modules:
    - typeAnatomy/visual-overlay
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - scripts/render-type-anatomy-overlays.ts
  out:
    - utils/typeAnatomy/
    - test/typeAnatomy/
    - test/utils/typeAnatomy/
    - public/fonts/
    - utils/geometry/
invariants:
  - The renderer's CASES list reflects the actual current detector behavior, not a historical bug state — visual-evidence accounting must not lag the test suite.
  - This slice does not change detector, geometry, topology, or accuracy-test files. Verification surfaces stay frozen; only the visual-overlay manifest is updated.
acceptance:
  - id: A1
    given: scripts/render-type-anatomy-overlays.ts after Slice 2 closed
    when: this slice lands
    then: |
      the i tittle case in CASES has label "i tittle" and expected: "pass"
      (was "i tittle miss" / "known-gap"); the H tittle case has label
      "H tittle (negative)" and expected: "pass" (was "H false tittle" /
      "known-gap"). All other CASES entries (H stems, e eye, Q tail,
      S spine, H crossbar, A crossbar) are untouched.
  - id: A2
    given: the updated renderer is run via tsx scripts/render-type-anatomy-overlays.ts
    when: it completes
    then: |
      the manifest table in stdout shows `'i tittle' count=1 expected='pass'`
      and `'H tittle (negative)' count=0 expected='pass'`. No row reports
      expected='known-gap' for any tittle case.
  - id: A3
    given: the slice's commit
    when: git diff --stat HEAD~1 is inspected
    then: |
      only scripts/render-type-anatomy-overlays.ts is modified (8 lines or
      fewer). No file under utils/typeAnatomy/, test/typeAnatomy/,
      test/utils/typeAnatomy/, public/fonts/, or utils/geometry/ appears
      in the diff.
non_functional:
  a11y: []
  perf: {}
  security: []
contracts: []
observability:
  logs: []
  metrics: []
  traces: []
migrations: []
rollback:
  - Revert the slice's commit; the overlay script returns to the post-Slice-2
    label state where i and H tittle cases are tagged "known-gap" despite
    the detector passing them. Documentation drift returns; runtime behavior
    is unaffected.
ai_assessment:
  confidence_level: 9
  uncertainty_areas:
    - None substantive. The renderer is presentation-only and has no test
      gate beyond manual eyeballing of the manifest table after the rerun.
  recommended_pairing: false

id: FIX-SLUG-INPUT-001
title: 'Keep author-typed hyphens in the dashboard slug field'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-21T03:35:08.097Z'
updated_at: '2026-09-21T03:45:47.054Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'dashboard content editor — new-content slug input and autosave path'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'app/dashboard/articles/new/components/ArticleMetadataForm.tsx'
    - 'app/dashboard/_components/NewContentEditor.tsx'
    - 'utils/slugify.ts'
    - 'utils/slugify.test.ts'
    - app/dashboard/articles/new/components/ArticleMetadataForm.test.tsx
  out: []
invariants:
  - 'Every hyphen an author types into the slug field survives in the field value'
  - 'Every slug persisted to Supabase satisfies ^[a-z0-9]+(?:-[a-z0-9]+)*$'
acceptance:
  - id: A1
    given: 'the new-content slug field'
    when: 'an author types a-b one character at a time'
    then: 'the field value is a-b and no typed hyphen is dropped'
  - id: A2
    given: 'the slug field holds a trailing hyphen'
    when: 'autosave persists the draft'
    then: 'the stored slug is trimmed to a valid slug and the save succeeds without an error'
non_functional: {}
contracts:
  - name: 'dashboard-slug-input'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Component test types \"domain-induced\" and \"a-b\" into the controlled slug field and asserts the value keeps every hyphen; it fails on the unfixed code (field became \"ab\"). The unit test covers the character-by-character sequence. Confirmed end-to-end on the fixed dev server through the dashboard: the field held \"slug-fix-end-to-end-check\" and the PATCH persisted that slug with HTTP 200. Raw run output is kept out of the repo at tmp/evidence/FIX-SLUG-INPUT-001/ (gitignored), so the citation is the test itself, not an artifact."
    recorded_at: "2026-09-21T03:46:00.181Z"
    test_nodeid: "app/dashboard/articles/new/components/ArticleMetadataForm.test.tsx > ArticleMetadataForm slug field > does not collapse a typed hyphen when the next character arrives"
    command: npx vitest run utils/slugify.test.ts app/dashboard/articles/new/components/ArticleMetadataForm.test.tsx
    exit_code: 0
    commit_sha: e03671c3cb2abad710dc06991dd6e834effbf093
  - criterion_id: A2
    status: pass
    evidence_ref: "Runtime observation on the fixed code (worktree dev server, localhost:3100): with the slug field focused and holding \"blur-normalization-probe-\" the autosave PATCH carried slug \"blur-normalization-probe\" and returned HTTP 200 — trimmed, not rejected. A second pass with \"trailing-hyphen-probe-\" behaved identically; blur then normalized the field. The save and publish payloads are built from slugify(snapshot.slug), so a partially typed trailing hyphen is never persisted or rejected. This is a runtime observation, not a re-derivable citation; the raw transcript is kept out of the repo at tmp/evidence/FIX-SLUG-INPUT-001/runtime-slug-verification.md."
    recorded_at: "2026-09-21T03:46:09.443Z"
    command: npx next dev --webpack -p 3100 from the worktree; dashboard autosave observed through the browser network log
    commit_sha: e03671c3cb2abad710dc06991dd6e834effbf093
id: FEAT-SCHEDULED-STATUS-001
title: 'Add the scheduled member to the article_status enum'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge sched-status-001 at f7eea390fb45e34ea9573464bc2e17d44d08adcb'
created_at: '2026-09-21T06:05:06.700Z'
updated_at: '2026-09-21T06:06:34.001Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'supabase schema — article_status enum'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'utils/supabase/migrations/add-scheduled-status.sql'
  out: []
invariants:
  - 'The migration is idempotent: re-running it is a no-op'
  - 'No application code relies on the new enum member yet, so behaviour is unchanged until the executor ships'
acceptance:
  - id: A1
    given: 'the migration file'
    when: 'it is inspected'
    then: 'it adds the scheduled member to public.article_status with IF NOT EXISTS and documents that the statement must run outside a transaction that uses the new value'
non_functional: {}
contracts:
  - name: 'publish-scheduling'
    type: schema
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Shape check on utils/supabase/migrations/add-scheduled-status.sql: exactly one ALTER TYPE public.article_status ADD VALUE IF NOT EXISTS 'scheduled' statement (idempotent), plus a documented rollback that explains Postgres cannot remove an enum member and gives the row-level retreat (set status back to draft and clear scheduled_at). The file also records that the statement must run outside a transaction that uses the new member. The member does not exist in the live database yet, so this is inspection evidence for an unapplied file."
    recorded_at: "2026-09-21T06:06:32.823Z"
    command: grep-based shape check on the migration file
    commit_sha: "85be04e07a74937fb3c41a44bd39dc06639865e7"
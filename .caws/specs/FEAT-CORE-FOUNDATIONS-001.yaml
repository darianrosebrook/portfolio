id: FEAT-CORE-FOUNDATIONS-001
title: 'Fill core-foundations stub pages with education-template content'
risk_tier: 3
mode: feature
lifecycle_state: active
worktree: feat-core-foundations-001
created_at: '2026-09-14T05:06:10.391Z'
updated_at: '2026-09-14T05:13:18.181Z'
created_by_session: 'session-ee97c70e-5708-43ac-898e-efaca06c09be'
blast_radius:
  modules:
    - 'app/blueprints/foundations (9 stub pages + shared education template)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'TODO: list the file(s) or directories this spec authorizes.'
    - app/blueprints/foundations/color/**
    - app/blueprints/foundations/spacing/**
    - app/blueprints/foundations/layout/**
    - app/blueprints/foundations/icons/**
    - app/blueprints/foundations/elevation/**
    - app/blueprints/foundations/radius/**
    - app/blueprints/foundations/motion/**
    - app/blueprints/foundations/grid/**
    - app/blueprints/foundations/borders/**
    - app/blueprints/foundations/_lib/**
    - app/blueprints/foundations/_components/**
    - types/foundationContent.ts
    - test/blueprints/**
    - test/e2e/foundationNavigation.spec.ts
  out: []
invariants:
  - 'Every filled page renders through the FoundationPage education template with complete FoundationPageContent (metadata, sections, crossReferences, verificationChecklist, assessmentPrompts) — no placeholder ''coming soon'' markup remains on any core-foundations page'
  - 'Token names, values, and contrasts cited in page content reference the repo''s real generated token system (tokens.css / design-token pipeline), not invented examples'
  - 'Static export metadata (title, description, OG/Twitter) is preserved or improved via generateFoundationMetadata; every page stays a server component exporting metadata'
acceptance:
  - id: A1
    given: >-
      the nine core-foundations stub pages (color, spacing, layout, icons,
      elevation, radius, motion, grid, borders)
    when: 'a filled page is rendered'
    then: >-
      it renders via FoundationPage with all template sections, exports Next
      metadata, and contains no 'coming soon' placeholder
  - id: A2
    given: 'page content citing token names, ramps, or contrast values'
    when: 'a reader compares them to the generated tokens.css artifacts'
    then: >-
      every cited token name exists in the real token system and contrast claims
      match computed WCAG results
  - id: A3
    given: 'the color and motion gold-standard pages'
    when: 'validated'
    then: >-
      typecheck, lint, and vitest pass with the pages included, and each page
      exercises the full section vocabulary of the education template
  - id: A4
    given: 'the remaining seven core-foundations pages'
    when: 'filled to the derived standard'
    then: >-
      each passes typecheck/lint/test and provides verification checklist +
      assessment prompts + cross-references like the gold-standard pages
non_functional: {}
contracts: []
evidence:
  - criterion_id: A3
    status: pass
    evidence_ref: "Color (b77a5724) + Motion (31b29e60) gold pages: npx tsc --noEmit exit 0; eslint clean; vitest test/blueprints 39/39 incl. 16 new assertions; e2e foundationNavigation color+motion all categories pass (32-passed run) incl. JSON-LD via server-rendered scripts (60d6fc6a); every template section type exercised incl. verification-checklist and additional-resources"
    recorded_at: "2026-09-14T05:46:39.544Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "Color+motion citations verified against ui/designTokens core+semantic sources and generated designTokens.scss by direct grep at authoring time (durations 50-1500ms, easings, stagger 17ms, ramp hexes, cascade-layer order); render tests assert exact token strings; WCAG pair ratios hand-computed and labeled approximate"
    recorded_at: "2026-09-14T05:46:40.028Z"
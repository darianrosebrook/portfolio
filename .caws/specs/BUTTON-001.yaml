id: BUTTON-001
type: refactor
title: Icon-only Button detection via :has()
status: closed
risk_tier: 3
mode: development
created_at: '2026-05-19T15:29:20.955Z'
updated_at: '2026-05-19T15:29:20.956Z'
blast_radius:
  modules:
    - ui/components/Button
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - ui/components/Button/
  out:
    - node_modules/
    - dist/
    - build/
invariants:
  - Button continues to render accessible name for icon-only buttons (via ariaLabel)
  - asChild rendering path remains functional for Popover.Trigger use cases
acceptance:
  - id: A1
    given: a Button with only an icon child (no text) and ariaLabel set
    when: rendered
    then: it has square aspect-ratio styling and exposes the ariaLabel as accessible name
  - id: A2
    given: a Button with string text children
    when: rendered
    then: text is wrapped in a span.label and the button keeps text-button padding
  - id: A3
    given: a Button using asChild with an icon-bearing trigger and ariaLabel
    when: rendered
    then: the trigger receives the button class and renders with icon-only styling
non_functional:
  a11y:
    - Icon-only buttons must have an accessible name via ariaLabel
  perf: {}
  security: []
contracts: []
worktree: button-icon-only

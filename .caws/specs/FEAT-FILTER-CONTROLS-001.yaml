id: FEAT-FILTER-CONTROLS-001
title: 'Use the Button primitive for the search and retry submit controls'
risk_tier: 2
mode: refactor
lifecycle_state: active
worktree: filter-controls-001
created_at: '2026-09-21T07:05:47.088Z'
updated_at: '2026-09-21T07:05:48.250Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'dashboard search and error-retry controls'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'app/dashboard/articles/_components/ArticleFilters.tsx'
    - 'app/dashboard/articles/page.tsx'
    - 'app/dashboard/case-studies/page.tsx'
    - 'app/dashboard/page.tsx'
    - 'app/dashboard/page.module.css'
  out: []
invariants:
  - 'Submitting the filter form still carries q, status and sort, and retry still navigates'
  - 'The submit label never wraps, which is the regression this control was fixed for earlier'
acceptance:
  - id: A1
    given: 'the articles filter view with a non-default filter applied'
    when: 'a query is submitted with the new control'
    then: >-
      the resulting URL keeps the filter and adds the query, so submission through
      the Button primitive still works
  - id: A2
    given: 'the search row at a narrow width'
    when: 'it renders'
    then: 'the submit control keeps its intrinsic width and its label stays on one line'
  - id: A3
    given: 'the dashboard source'
    when: 'raw submit controls are counted'
    then: 'the four raw applyButton buttons are gone and the applyButton rules are removed from the module without leaving an unreferenced class'
non_functional: {}
contracts:
  - name: 'dashboard-search'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Runtime on the worktree build: from /dashboard/articles?status=draft the form carried hidden status=draft, and activating the control (a Button rendered as type=submit) navigated to /dashboard/articles?status=draft&q=prime — the non-default filter rides along and the query is added. The default view submits as /dashboard/articles?q=prime, which is also what the pre-change build did, so the hidden-input behaviour is unchanged: those inputs render only for non-default filters."
    recorded_at: "2026-09-21T07:08:58.564Z"
    command: "browser: submit the filter form with and without a non-default filter and read the resulting URL"
    commit_sha: "2ffbc1634f294c1ace7a28d0d6b1ea529606ca75"
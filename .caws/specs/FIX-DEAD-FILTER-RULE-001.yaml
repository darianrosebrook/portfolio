id: FIX-DEAD-FILTER-RULE-001
title: 'Remove the filter rule whose select matches nothing'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge dead-filter-rule-001 at ed80bcb233e7ae6804b0df293b660cbba24d4f24'
created_at: '2026-09-21T07:29:57.377Z'
updated_at: '2026-09-21T07:31:45.995Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'dashboard filter form — stylesheet hygiene'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'app/dashboard/page.module.css'
  out: []
invariants:
  - 'The search field renders identically; every declaration in the removed rule is supplied by the primitive or a global rule'
  - 'The focus indicator is untouched'
acceptance:
  - id: A1
    given: 'the filter form'
    when: 'the rule that styled its input and select children is removed'
    then: 'the search field computed padding, background, border, radius, min-width, colour and font are unchanged'
  - id: A2
    given: 'the dashboard'
    when: 'the filter fields are searched for a select element'
    then: 'none exists, so the select half of the rule matched nothing'
  - id: A3
    given: 'the stylesheet after the change'
    when: 'the CSS-usage checker and the suite run'
    then: 'they are clean'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Computed styles on the search field are identical before and after, measured at the same 768x1400 viewport on both builds: padding-inline 12px, padding-block 0px, background rgb(0,0,0), border 1px solid rgb(85,85,85), radius 8px, min-width 0px, colour rgb(250,250,250), font-family Inter fallback chain, font-size 18px, and the rendered box 316x48. The differing-properties list is empty and the rect is unchanged, so removing the rule is appearance-neutral: every declaration it carried is supplied by the Input primitive tokens or by the global reset and input/textarea rules."
    recorded_at: "2026-09-21T07:31:43.655Z"
    command: "browser: read the field computed styles on the pre-change and post-change builds at the same viewport"
    commit_sha: d9f6a712654474f61402f8cc03d35066e8167b5a
  - criterion_id: A2
    status: pass
    evidence_ref: "grep shows .filterFields is used in exactly one place, ArticleFilters.tsx, and that file contains zero select elements — the runtime check agrees, reporting 0 selects inside the filter fields and 0 selects on the page. So the rule select half matched no element. This is the class of dead CSS the usage checker cannot see: it tracks whether a class name is referenced, and .filterFields is referenced, so the unreachable selector stayed invisible to it."
    recorded_at: "2026-09-21T07:31:44.294Z"
    command: "grep -rn filterFields app/dashboard --include=*.tsx; browser: count selects inside the filter fields"
    commit_sha: d9f6a712654474f61402f8cc03d35066e8167b5a
  - criterion_id: A3
    status: pass
    evidence_ref: "The CSS-usage checker reports 0 unreferenced classes across 19 modules with exit 0, and the full suite passed at 2070 with 13 expected failures and 2 skips, exit 0. The input focus outline rule was deliberately left in place — the post-write check confirms .filterFields input:focus-visible is still declared and still reachable in the loaded stylesheets, so the focus indicator is untouched."
    recorded_at: "2026-09-21T07:31:44.948Z"
    command: node scripts/check-css-module-usage.mjs; npm test; grep for the retained focus selector
    exit_code: 0
    commit_sha: d9f6a712654474f61402f8cc03d35066e8167b5a
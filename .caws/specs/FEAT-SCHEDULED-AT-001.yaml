id: FEAT-SCHEDULED-AT-001
title: 'Author the scheduled_at migration that publish scheduling needs'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge sched-at-001 at ba8624c8d9965267e0fd240f129dc96deab68189'
created_at: '2026-09-21T06:02:26.650Z'
updated_at: '2026-09-21T06:04:35.517Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'supabase schema — articles and case_studies publish scheduling'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'utils/supabase/migrations/add-scheduled-at.sql'
  out: []
invariants:
  - 'The migration is additive and idempotent: every clause uses IF NOT EXISTS'
  - 'No application code reads scheduled_at, so the dashboard behaves identically until the migration is applied'
  - 'The file carries its own rollback so the change can be undone'
acceptance:
  - id: A1
    given: 'the migration file'
    when: 'it is inspected'
    then: 'it adds a nullable scheduled_at timestamptz to articles and case_studies with ADD COLUMN IF NOT EXISTS, creates one partial index per table over non-null scheduled_at, and documents rollback statements in comments'
  - id: A2
    given: 'the application as it stands'
    when: 'the dashboard renders with the migration unapplied'
    then: 'no code path references scheduled_at and the full test suite still passes'
non_functional: {}
contracts:
  - name: 'publish-scheduling'
    type: schema
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Shape check on utils/supabase/migrations/add-scheduled-at.sql: 2 ADD COLUMN IF NOT EXISTS scheduled_at timestamptz (articles, case_studies), 2 CREATE INDEX IF NOT EXISTS, 2 partial WHERE scheduled_at IS NOT NULL clauses, and 4 commented rollback statements (2 index drops, 2 column drops). A line-based guard scan reported 2 statements without IF NOT EXISTS; both are the ALTER TABLE header lines whose guard sits on the continuation line, so the DDL is fully guarded. Separately verified against the live project through the REST schema: both tables now expose scheduled_at as timestamp with time zone (this file was applied by the user out of band, and the file records what was applied)."
    recorded_at: "2026-09-21T06:04:33.378Z"
    command: grep-based shape check + REST schema read (GET /rest/v1/ with the service key)
    commit_sha: abea68f676fceef6b190bc717788e601e8880b3f
  - criterion_id: A2
    status: pass
    evidence_ref: "No application code reads the column yet: grep for scheduled_at across app/ and utils/ returns 0 hits at this revision, and the full suite passes unchanged (210 files, 2053 passed, 13 expected fail, 2 skipped, exit 0), so dashboard behaviour is identical to before the schema change until the UI slice lands."
    recorded_at: "2026-09-21T06:04:34.072Z"
    command: "grep -rn scheduled_at app utils | wc -l; npm test"
    exit_code: 0
    commit_sha: abea68f676fceef6b190bc717788e601e8880b3f
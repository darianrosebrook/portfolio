id: FEAT-SCHEDULED-WRITE-001
title: 'Let an update schedule content instead of publishing it'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge sched-write-001 at 89b62a3f66b37e4855bfc1f788b5a5df8a3fd9d0'
created_at: '2026-09-21T06:33:43.821Z'
updated_at: '2026-09-21T06:37:26.017Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'content update schemas and publish path'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'utils/schemas/article.schema.ts'
    - 'utils/schemas/case-study.schema.ts'
    - 'test/utils/schemas/publish-payload-repro.test.ts'
    - 'test/utils/schemas/case-study.schema.test.ts'
  out: []
invariants:
  - 'A scheduled update stores the status and the moment, and publishes nothing'
  - 'The working draft is promoted only on an actual publish, never on a schedule'
  - 'The scheduled status is accepted only because the database enum carries it'
acceptance:
  - id: A1
    given: 'an update payload carrying status scheduled and a scheduled_at'
    when: 'the update schema parses it'
    then: 'the status is accepted and the timestamp is retained rather than stripped'
  - id: A2
    given: 'a scheduled update through the route'
    when: 'it is applied'
    then: 'the row holds status scheduled and scheduled_at, the canonical body is left alone, and is_dirty stays as it was'
  - id: A3
    given: 'a draft scheduled in the past'
    when: 'the executor runs with the cron token'
    then: 'the row becomes published with published_at equal to the scheduled instant'
non_functional: {}
contracts:
  - name: 'publish-scheduling'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Both update schemas accept status \"scheduled\" and retain scheduled_at. The tests failed before the change with \"expected false to be true\" (the parse rejected the status), and pass after: result.data.status is \"scheduled\" and result.data.scheduled_at equals the sent instant, while the second case pins that scheduled_at stays optional so an ordinary update still parses. 27 tests pass across the three schema suites."
    recorded_at: "2026-09-21T06:37:24.032Z"
    test_nodeid: "test/utils/schemas/publish-payload-repro.test.ts > updateArticleSchema — scheduling > accepts the scheduled status and keeps the moment"
    command: npx vitest run test/utils/schemas/
    exit_code: 0
    commit_sha: "20251bb62315a8f72cbe5b3ece4b96ee578126a4"
  - criterion_id: A2
    status: pass
    evidence_ref: "Against the live database with a throwaway article: POST created it as a draft (201), then PUT {status: scheduled, scheduled_at: 2026-09-01T09:00:00.000Z} returned 200 and the stored row read back status \"scheduled\", scheduled_at \"2026-09-01T09:00:00+00:00\" and published_at null — nothing was published. Gap: the probe had no divergent working draft, so promotion of working fields over canonical was not exercised on this path; the observable that matters (published_at still null, status still scheduled) was checked."
    recorded_at: "2026-09-21T06:37:24.581Z"
    command: "browser: POST /api/articles then PUT /api/articles/<slug> against the worktree dev server"
    commit_sha: "20251bb62315a8f72cbe5b3ece4b96ee578126a4"
  - criterion_id: A3
    status: pass
    evidence_ref: "Same run: GET /api/cron/publish-scheduled with Authorization: Bearer <CRON_SECRET> returned 200 {\"count\":1,\"published\":[{\"table\":\"articles\",\"slug\":\"schedule-probe-…\",\"published_at\":\"2026-09-01T09:00:00+00:00\"}],\"failures\":[]}, and the row then read back status \"published\" with published_at equal to the stored scheduled_at string. My own flag reported false only because it compared against the client-sent \".000Z\" form rather than the stored value — the same string-vs-instant trap as earlier in this work; the two stored strings are identical. The probe row was deleted afterwards (204)."
    recorded_at: "2026-09-21T06:37:25.139Z"
    command: "browser: GET /api/cron/publish-scheduled + GET /api/articles/<slug> + DELETE"
    commit_sha: "20251bb62315a8f72cbe5b3ece4b96ee578126a4"
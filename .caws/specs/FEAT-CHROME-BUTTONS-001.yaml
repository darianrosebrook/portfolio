id: FEAT-CHROME-BUTTONS-001
title: 'Use the Button primitive for the metadata toggle and the preview close'
risk_tier: 2
mode: refactor
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge chrome-buttons-001 at d2a3473357dd7a8ce221b9a2657c625b866aa9f6'
created_at: '2026-09-21T07:16:12.236Z'
updated_at: '2026-09-21T07:18:25.663Z'
created_by_session: 'session-103ffb94-b7a9-41b1-81cb-2f9d0e7a187b'
blast_radius:
  modules:
    - 'dashboard new-content flow — toolbar and preview chrome'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'app/dashboard/articles/new/components/EditorLayout.tsx'
    - 'app/dashboard/articles/new/components/EditorLayout.module.css'
    - 'app/dashboard/articles/new/components/ArticlePreview.tsx'
    - 'app/dashboard/articles/new/components/ArticlePreview.module.css'
  out: []
invariants:
  - 'Toggling still shows and hides the metadata sidebar and flips its label'
  - 'Closing still dismisses the preview overlay'
acceptance:
  - id: A1
    given: 'the editor toolbar'
    when: 'the metadata toggle is activated'
    then: 'the sidebar hides and the control reads Show Metadata, and activating it again restores both'
  - id: A2
    given: 'the open preview'
    when: 'Close Preview is activated'
    then: 'the overlay is dismissed'
  - id: A3
    given: 'both components'
    when: 'the source is inspected'
    then: 'neither renders a raw button element and neither module retains a class the primitive replaced'
non_functional: {}
contracts:
  - name: 'dashboard-editor'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Runtime on the new-content editor: the toggle renders with class \"button small secondary\" (the primitive) and the label Hide Metadata with the sidebar present. Activating it hid the sidebar and flipped the label to Show Metadata; activating it again restored the sidebar and the Hide Metadata label. So both the behaviour and the label flip survive the swap."
    recorded_at: "2026-09-21T07:18:23.550Z"
    command: "browser: activate the metadata toggle twice and read the sidebar presence and label each time"
    commit_sha: "078f2d0e64bcff1f3be4f56a14a5525c37866620"
  - criterion_id: A2
    status: pass
    evidence_ref: "Runtime: typing into the editor enabled Preview; activating it opened the overlay and the close control rendered as class \"button medium secondary\" with the label Close Preview. Activating it removed the overlay element entirely. The open overlay still reported 0 inline styles, so the earlier conversion of the overlay chrome is intact."
    recorded_at: "2026-09-21T07:18:24.140Z"
    command: "browser: open the preview from the new-content editor and activate Close Preview"
    commit_sha: "078f2d0e64bcff1f3be4f56a14a5525c37866620"
  - criterion_id: A3
    status: pass
    evidence_ref: "grep -c \"<button\" returns 0 for both EditorLayout.tsx and ArticlePreview.tsx, and the names closeButton and metadataToggle appear nowhere in either component or either module. The CSS-usage checker reports 0 unreferenced classes across 19 modules, so removing those rules left nothing dead, and the full suite passed at 2070. CORRECTION to the commit message: it claims the checker would have caught the first failed patch. It would not have — the checker reports classes defined but never referenced, whereas the broken state was a class referenced but no longer defined, which passes typecheck because CSS-module types are not per-class here. What actually surfaced the failure was the patch script printing \"anchor not found\" to stderr and the follow-up grep showing the raw button still present."
    recorded_at: "2026-09-21T07:18:24.737Z"
    command: "grep -c \"<button\" on both components; grep for the two class names; node scripts/check-css-module-usage.mjs"
    exit_code: 0
    commit_sha: "078f2d0e64bcff1f3be4f56a14a5525c37866620"
id: FIX-010
title: 'Destroy the Tiptap editor in TableOfContentsExtension test so npm test exits 0'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge fix-010-editor-teardown at c5cb814466997ef25c281ce541f50282aa93f59a'
created_at: '2026-08-06T14:02:01.084Z'
updated_at: '2026-08-06T14:05:52.433Z'
blast_radius:
  modules:
    - test/components
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'test/components/TableOfContentsExtension.test.ts'
  out: []
invariants:
  - A test that constructs a Tiptap Editor releases it before the environment tears down
  - The assertion this test makes about the toc node is unchanged
acceptance:
  - id: A1
    given: the test constructs a Tiptap Editor and never destroys it, so prosemirror-view's DOMObserver flush timer fires after jsdom teardown
    when: the full suite runs
    then: 'vitest reports an unhandled "ReferenceError: document is not defined" and npm test exits 1 despite every test passing'
  - id: A2
    given: the editor is released once the assertion has been made
    when: the full suite is re-run
    then: vitest reports 0 errors and npm test exits 0, with the same test count passing as before
non_functional: {}
contracts: []

id: CONTRACTS-002
title: Close enforcement gaps from CONTRACTS-001 audit
risk_tier: 3
mode: chore
created_at: '2026-05-04T18:47:02.787Z'
updated_at: '2026-05-04T18:47:02.788Z'
blast_radius:
  modules:
    - scripts/validateComponents.mjs
    - scripts/validateComponentLayers.mjs
    - ui/components/
    - ui/modules/
  data_migration: false
operational_rollback_slo: 30m
scope:
  in:
    - scripts/validateComponents.mjs
    - scripts/validateComponentLayers.mjs
    - ui/components/AlertNotice/index.tsx
    - ui/components/ToggleSwitch/index.tsx
    - ui/components/breadcrumbs/index.tsx
    - ui/components/links/index.tsx
    - ui/components/BrandSwitcher/
    - ui/components/Chip/
    - ui/components/AnimatedCard/
    - ui/components/AnimatedSection/
    - ui/components/AnimatedText/
    - ui/modules/CodeSandbox/
    - ui/modules/Editor/
    - ui/modules/ErrorBoundary/
    - ui/modules/FontInspector/
    - ui/modules/Tiptap/
  out:
    - ui/designTokens/component.contract.schema.json
    - scripts/validate-contracts.mjs
    - scripts/check-kebab-token-vars.mjs
    - scripts/check-contract-traceability.mjs
    - test/utils/contractTest.ts
    - utils/designTokens/
invariants:
  - npm run contracts:validate exits 0 (53/53 pass) — must not regress
  - npm run tokens:lint exits 0 — must not regress
  - npm run contracts:traceability exits 0 — must not regress
  - All existing passing tests continue to pass
acceptance:
  - id: A1
    given: >-
      validateComponents.mjs is run against AlertNotice, ToggleSwitch, breadcrumbs, links,
      BrandSwitcher
    when: each component has a .ts or .tsx index file with any valid default-export pattern
    then: each component reports 0 errors
  - id: A2
    given: validateComponents.mjs is run against ErrorBoundary, FontInspector, Tiptap
    when: those modules use named re-exports instead of export-default shape
    then: each module reports 0 errors
  - id: A3
    given: validateComponents.mjs is run against the full components and modules directories
    when: >-
      AnimatedCard, AnimatedSection, AnimatedText, BrandSwitcher, and Chip have README.md and tests/
      stubs
    then: the validator exits with 0 errors
  - id: A4
    given: >-
      validateComponents.mjs is run against components that have contract.json with a non-composer
      layer
    when: those components import from a Context module
    then: they are not classified as composers and pass with 0 errors
non_functional:
  security: []
  accessibility: []
  performance: []
contracts: []
worktree: contracts-001
lifecycle_state: closed
resolution: completed

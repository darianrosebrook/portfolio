id: FIX-ERRBND-001
title: Narrow try/catch in darianrosebrook page to data fetch only
risk_tier: 3
mode: fix
created_at: '2026-05-19T16:16:22.728Z'
updated_at: '2026-05-19T16:16:22.729Z'
worktree: fix-errbnd
blast_radius:
  modules:
    - app/darianrosebrook
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - app/darianrosebrook/page.tsx
  out:
    - node_modules/
    - dist/
    - build/
invariants:
  - >-
    Error path still renders the fallback "Unable to load Bluesky profile" UI when
    fetchBlueskyData() throws
  - Happy path renders identical DOM structure to before
acceptance:
  - id: A1
    given: the closed historical change
    when: the work was completed
    then: All 22 react-hooks/error-boundaries errors are gone from `npm run lint`
  - id: A2
    given: the closed historical change
    when: the work was completed
    then: >-
      When fetchBlueskyData() throws, the page renders the error fallback (h1 + p in
      styles.errorContainer)
  - id: A3
    given: the closed historical change
    when: the work was completed
    then: >-
      When fetchBlueskyData() resolves successfully, the page renders the same profile header,
      description, and posts section as before
non_functional:
  security: []
  accessibility: []
  performance: []
contracts: []
lifecycle_state: closed
resolution: completed

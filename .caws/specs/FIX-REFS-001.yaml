id: FIX-REFS-001
title: Eliminate ref-access-during-render across components and utilities
risk_tier: 3
mode: fix
created_at: '2026-05-19T17:23:23.558Z'
updated_at: '2026-05-19T17:23:23.559Z'
worktree: fix-refs
blast_radius:
  modules:
    - app/heroes
    - ui/components/Accordion
    - ui/components/AnimatedCard
    - ui/components/AnimatedSection
    - ui/components/AnimatedText
    - ui/components/Tooltip
    - ui/components/Truncate
    - ui/modules/FontInspector
    - utils/animation
    - utils/focus
  data_migration: false
operational_rollback_slo: 30m
scope:
  in:
    - app/heroes/glossaryCards.tsx
    - ui/components/Accordion/Accordion.tsx
    - ui/components/AnimatedCard/AnimatedCard.tsx
    - ui/components/AnimatedSection/AnimatedSection.tsx
    - ui/components/AnimatedText/AnimatedText.tsx
    - ui/components/Tooltip/Tooltip.tsx
    - ui/components/Truncate/Truncate.tsx
    - ui/modules/FontInspector/FeatureCoachmark.tsx
    - utils/animation/index.ts
    - utils/focus/index.ts
  out:
    - node_modules/
    - dist/
    - build/
invariants:
  - GSAP and intersection-observer animations continue to fire on mount and on scroll
  - Forwarded refs from parents still receive the underlying DOM node after mount
  - Tooltip continues to position itself relative to its trigger element
  - Accordion content collapses and expands with the correct height transition
  - Focus trap continues to return the current set of focusable elements when consumers query it
acceptance:
  - id: A1
    given: the closed historical change
    when: the work was completed
    then: All 17 react-hooks/refs errors are gone from `npm run lint`
  - id: A2
    given: the closed historical change
    when: the work was completed
    then: >-
      Existing component tests for Accordion, AnimatedCard, AnimatedSection, AnimatedText, Tooltip,
      Truncate, FocusTrap all pass
  - id: A3
    given: the closed historical change
    when: the work was completed
    then: >-
      glossaryCards width-based slicing actually responds to container width (not just stuck on the
      initial render's null check)
  - id: A4
    given: the closed historical change
    when: the work was completed
    then: Accordion content height animation works on first open from default-closed state
non_functional:
  security: []
  accessibility: []
  performance: []
contracts: []
lifecycle_state: closed
resolution: completed

{
  "prefix": "icon",
  "tokens": {
    "color": {
      "foreground": {
        "default": "{semantic.color.foreground.primary}"
      }
    },
    "size": {
      "padding": {
        "default": "{core.size.01}"
      }
    }
  }
}

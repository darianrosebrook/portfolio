{
  "prefix": "popover",
  "tokens": {
    "size": {
      "padding": {
        "default": "{core.size.06}"
      },
      "radius": {
        "default": "{semantic.size.radius.02}"
      }
    },
    "color": {
      "background": {
        "content": "{semantic.color.background.secondary}"
      },
      "border": {
        "accent": "{semantic.color.border.accent}"
      }
    },
    "elevation": {
      "default": "{semantic.elevation.default}"
    }
  }
}

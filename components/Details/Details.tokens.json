{
  "prefix": "details",
  "tokens": {
    "size": {
      "padding": {
        "default": "{core.size.06}"
      },
      "radius": {
        "default": "{semantic.size.radius.02}"
      }
    },
    "color": {
      "background": {
        "default": "{semantic.color.background.primary}",
        "subtle": "{semantic.color.background.secondary}",
        "hover": "{semantic.color.background.hover}",
        "code": "{semantic.color.background.secondary}"
      },
      "foreground": {
        "primary": "{semantic.color.foreground.primary}",
        "secondary": "{semantic.color.foreground.secondary}"
      },
      "border": {
        "default": "{semantic.color.border.primary}",
        "hover": "{semantic.color.border.bold}",
        "accent": "{semantic.color.border.accent}"
      }
    },
    "focus": {
      "ring": {
        "width": "{semantic.focus.ring.border.width}",
        "style": "{semantic.focus.ring.border.style}",
        "color": "{semantic.focus.ring.border.color}",
        "offset": "{semantic.focus.ringOffset}"
      }
    }
  }
}

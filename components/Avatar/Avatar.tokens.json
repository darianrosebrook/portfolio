{
  "prefix": "avatar",
  "tokens": {
    "size": {
      "radius": {
        "default": "{semantic.size.radius.full}"
      }
    },
    "color": {
      "border": {
        "default": "{semantic.color.border.light}"
      }
    }
  }
}
